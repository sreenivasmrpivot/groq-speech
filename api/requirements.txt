# API Server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Note: groq_speech package is installed via the root setup.py
# when running pip install -e . from the project root
